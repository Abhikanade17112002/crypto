// Loader.js
import React from 'react';
import './Loader.css';  // Add CSS for loader

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
  </div>
);

export default Loader;
